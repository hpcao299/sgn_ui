const routes = {
    home: '/',

    products: '/products',
    productsDetails: '/products/:slug',

    cart: '/shopping-cart',

    login: '/login',
    signup: '/signup',
};

export default routes;
